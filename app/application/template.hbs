<nav>
	{{#link-to "index"}}
		Index
	{{/link-to}}
	{{#if session.isAuthenticated}}
		<span>
			{{sessionAccount.currentUser.username}}
			<button {{action "logout"}}>Logout</button>
		</span>
		{{#link-to 'secret'}}Secrets{{/link-to}}
	{{else}}
		{{#link-to "login"}}
			Login
		{{/link-to}}
		{{#link-to "signup"}}
			Sign up
		{{/link-to}}
	{{/if}}
</nav>
{{outlet}}
